import { useApi } from "./api.logic.data.handling.hook";
import { useApiStatus } from "./api.status.hook";

/**react-design-patterns-app - version 21.20 - index > hooks
 * - Features: 
 * 
 *     --> importing and exporting 'useApi' 
 * 
 * Note: this component will use a helper function
*/

export { 
    useApiStatus,
    useApi
}